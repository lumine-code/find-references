{
  "name": "find-references",
  "author": "lumine-code",
  "description": "Highlight and list references to the symbol under the cursor.",
  "repository": "https://github.com/lumine-code/find-references",
  "bugs": {
    "url": "https://github.com/lumine-code/find-references/issues"
  },
  "keywords": [
    "usages",
    "occurrences",
    "call-sites",
    "symbol",
    "highlight",
    "lsp"
  ],
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "lumine": "^1.0.0"
  },
  "backgroundTips": [
    "{% if keys['find-references:show-panel'] %}You can list every reference to the symbol under the cursor with {{ 'find-references:show-panel' | keystroke }}{% else %}You can list every reference to the symbol under the cursor, grouped by file.{% endif %}"
  ],
  "main": "./lib/main",
  "files": [
    "docs",
    "lib",
    "spec",
    "styles"
  ],
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "consumedServices": {
    "find-references.provider": {
      "versions": {
        "^1.0.0": "consumeFindReferences"
      }
    }
  },
  "providedServices": {
    "find-references.markers": {
      "description": "Reports the reference occurrence markers currently highlighted in each editor.",
      "versions": {
        "1.0.0": "provideFindReferencesMarkers"
      }
    }
  },
  "dependencies": {
    "@lumine-code/etch": "github:lumine-code/etch#ab36096d3ec06484e8d6ed18402554879648831d",
    "picomatch": "^4.0.5"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.2.2",
    "globals": "^17.7.0",
    "prettier": "^3.9.6"
  },
  "configSchema": {
    "autoHighlight": {
      "title": "Auto Highlight",
      "description": "Highlight references to the symbol under the cursor automatically whenever the cursor rests. When disabled, references are highlighted only via the `find-references:highlight` command.",
      "type": "boolean",
      "default": true
    },
    "delay": {
      "title": "Delay",
      "description": "How long, in milliseconds, the cursor must rest before references are requested.",
      "type": "integer",
      "minimum": 0,
      "default": 400
    },
    "skipCurrentReference": {
      "title": "Skip Current Reference",
      "description": "Do not highlight the reference currently under the cursor.",
      "type": "boolean",
      "default": true
    },
    "ignoreThreshold": {
      "title": "Ignore Threshold",
      "description": "Skip highlighting when the ratio of references to buffer lines exceeds this value, e.g. when the cursor lands on a mundane token like `true`. Zero disables the limit.",
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "default": 0
    },
    "splitDirection": {
      "title": "Split Direction",
      "description": "Direction to split the active pane when the results panel opens. With `none` the panel opens in the active pane.",
      "type": "string",
      "enum": [
        "none",
        "right",
        "down"
      ],
      "default": "none"
    }
  }
}
